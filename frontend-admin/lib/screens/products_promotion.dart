import 'package:flutter/material.dart';

class ProductsPromotion extends StatelessWidget {
  const ProductsPromotion({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Products Promotion'),
    );
  }
}
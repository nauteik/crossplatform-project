import 'package:flutter/material.dart';

class BuildConfigurationPage extends StatelessWidget {
  const BuildConfigurationPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Build Configuration Page'),
    );
  }
}
import 'package:flutter/material.dart';

class PCGamingPage extends StatelessWidget {
  const PCGamingPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('PC Gaming Page'),
    );
  }
}
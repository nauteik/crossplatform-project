import 'package:flutter/material.dart';

class MousePage extends StatelessWidget {
  const MousePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Mouse Page'),
    );
  }
}
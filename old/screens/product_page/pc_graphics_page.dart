import 'package:flutter/material.dart';

class PCGraphicsPage extends StatelessWidget {
  const PCGraphicsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('PC Graphics Page'),
    );
  }
}
import 'package:flutter/material.dart';

class PCOfficePage extends StatelessWidget {
  const PCOfficePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('PC Office Page'),
    );
  }
}
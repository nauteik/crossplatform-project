import 'package:flutter/material.dart';

class BuildConfigurationScreen extends StatelessWidget {
  const BuildConfigurationScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Xây dựng cấu hình'),
    );
  }
} 
import 'package:flutter/material.dart';

class PCGamingScreen extends StatelessWidget {
  const PCGamingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('PC Gaming'),
    );
  }
} 